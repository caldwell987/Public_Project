<h1>Users:</h1>
<% @selectedusers.each do |i| %>
<li>
    <%= link_to i.firstname, profile_path(i.username) %>
 </li>
<% end %>

<%= console %>


<% for user in @users %>
  <div class="user">
    <p>
      <strong><%=h user.username %></strong>
      <%= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post %>
      <div class="clear"></div>
    </p>
  </div>
<% end %>


<h2>Your Friends</h2>
<ul>
  <%# for user in @user.friends %>
  <li><%# h user.username %></li>
  <%# end %>  
</ul>